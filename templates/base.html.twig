<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block body %} <button onclick="document.location='/showUsers'">Просмотр всех пользователей</button>
        {% endblock %}
        Hello? World!




{% for item in items %}
          Name: {{item.name | upper}} Id: {{item.id}} <br>
        {% endfor %}

        {% block content %}
          <h1>Block content text </h1>
        {% endblock %}



    </body>
</html>

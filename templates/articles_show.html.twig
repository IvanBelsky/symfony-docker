{% extends 'base.html.twig' %}
{% import "macro/core.twig" as core %}
{% block body %}
  <br/> <br/>
Таблица всех статей
<table>
  {% for item in items %}
    <tr>
      <td>Name: {{item.name | upper}}</td>

      <td><form action="{{ path('show_comments_by_id', { 'id': item.id, 'page':1}) }}"
                method="POST">
          <button>show comments</button>
        </form>
      </td>

    </tr>
  {% endfor %}
</table>
  <br/> <br/>
{% endblock %}

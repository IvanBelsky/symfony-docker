{% extends 'base.html.twig' %}
{% import "macro/core.twig" as core %}
{% block body %}
  <br/> <br/>
Таблица всех статей <br/>
<table>
  {% for item in items %}
    <tr>
      <td> {{item.name}}</td>

      <td><form action="{{ path('show_comments_by_id', { 'id': item.id, 'page':page}) }}"
                method="POST">
          <button>show comments</button>
        </form>
      </td>

    </tr>
  {% endfor %}
</table>
  <br/> <br/>
{% endblock %}
